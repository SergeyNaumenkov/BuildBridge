{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Build",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:buildbridge.getExePath}",
            "cwd": "${workspaceFolder}/",
            "preLaunchTask": "${command:buildbridge.getBuildTask}"
        }
    ]
}